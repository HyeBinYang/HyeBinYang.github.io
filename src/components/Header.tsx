import React from "react";
import { colors } from "../style";

const Header = () => {
  return <header css={{ width: "100vw", height: "80px", backgroundColor: colors.primary }}></header>;
};

export default Header;
